export enum TSubscription { //-- suplify
    none = 'none', // This can not be created through admin dashboard
    freeTrial = 'freeTrial', // This can not be created through admin dashboard // we assign standard price Id here 
    standard = 'standard',
    standardPlus = 'standardPlus',
    vise = 'vise'
}
