export enum QuestionType {
  document = 'document',
  image = 'image',
  unknown = 'unknown',
}

export enum TQuestionAnswer {
  text = 'text',
  number = 'number',
  single = 'single',
  multi = 'multi',
}