export enum TWithdrawalRequst {
  requested = 'requested',
  processing = 'processing',
  completed = 'completed',
  failed = 'failed',
  rejected = 'rejected',
}

export enum TRequstStatus {
  accept = 'accept',
  reject = 'reject',
}